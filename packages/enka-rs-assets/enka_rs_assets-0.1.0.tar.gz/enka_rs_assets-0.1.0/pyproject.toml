[project]
name = "enka-rs-assets"
version = "0.1.0"
description = "A repo that generates and stores the assets for enka-rs"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["aiofiles>=24.1.0", "aiohttp>=3.11.3", "orjson>=3.10.11"]
authors = [{ name = "MocA-Love", email = "maguro@maguro-bot.com" }]
license = { file = "LICENSE" }

[tool.setuptools]
packages = ["json_cooker"]