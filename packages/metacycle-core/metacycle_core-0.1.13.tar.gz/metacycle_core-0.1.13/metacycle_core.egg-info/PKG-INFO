Metadata-Version: 2.2
Name: metacycle-core
Version: 0.1.13
Summary: python backend server for metacycle, interacting with bluetooth accessories and controlling carla simulator.
Requires-Python: ~=3.10
Description-Content-Type: text/markdown
Requires-Dist: aiohttp-cors>=0.7.0
Requires-Dist: aiohttp>=3.11.11
Requires-Dist: python-socketio>=5.12.1
Requires-Dist: bleak>=0.22.3
Requires-Dist: rich>=13.9.4
Requires-Dist: carla==0.9.15
Requires-Dist: pygame>=2.6.1
Requires-Dist: numpy>=2.2.2
Requires-Dist: opencv-python>=4.11.0.86

# Development

```
uv sync
```

We use ruff linter.
```
uvx ruff check
```

# Run

```
uv run metacycle-hello
```

# Upload to PyPI and then run as as a tool

Update version number in `pyproject.toml`

This is how end users will run the server in a single line.

```
rm -r dist/
uv build
uv publish --token $TOKEN
```

Open another terminal (wait a moment for PyPI to be updated):
```
uvx --from metacycle-core@latest metacycle-hello
```
