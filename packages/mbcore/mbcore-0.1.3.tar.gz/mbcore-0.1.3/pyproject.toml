[project]
name = "mbcore"
version = "0.1.3"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mbodiai team", email = "info@mbodi.ai" },
]
requires-python = ">=3.10"
dependencies = [
    "rich-click>=1.8.5",
    "rich",
]

[project.scripts]
mbcore = "mbcore.cli:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project.optional-dependencies]
dev = [
    "pytest>=8.3.4",
    "pytest-asyncio",
]

