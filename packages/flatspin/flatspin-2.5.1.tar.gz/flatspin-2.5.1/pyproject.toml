[build-system]
requires = [
    "setuptools >= 40.6.0",
    "wheel",
    "setuptools_scm>=6.2",
    "setuptools_scm_git_archive",
    ]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "flatspin/_version.py"
