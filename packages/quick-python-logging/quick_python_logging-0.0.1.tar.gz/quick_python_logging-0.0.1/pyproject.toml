[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "quick_python_logging"
version = "0.0.1"
authors = [
  { name="Alex Dickens", email="alxdickens@gmail.com" },
]
description = "LogIt - The best python logging package"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = "MIT"
license-files = ["LICEN[CS]E*"]

[project.urls]
