[build-system]
requires = ["setuptools", "wheel", "pybind11"]
build-backend = "setuptools.build_meta"

[project]
name = "cm-tpm"
version = "0.1.3"
description = "A Python package for Continuous Mixtures of Tractable Probabilistic Models"
dependencies = [
    
]
