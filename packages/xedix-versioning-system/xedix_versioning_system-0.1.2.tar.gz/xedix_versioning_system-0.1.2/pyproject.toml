[build-system]
requires = ["setuptools>=45", "setuptools_scm[toml]>=6.2"]
build-backend = "setuptools.build_meta"

[project]
name = "xedix-versioning-system"
version = "0.1.2"
authors = [
    {name = "Juraj Kollár", email = "mostypc7@gmail.com"}
]
description = "A simple version control system"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]

[project.scripts]
xvs = "xvs.cli:main"
