[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "verifiable-rewards"
version = "0.0.1"
description = "Computing verifirable rewards."
authors = [{ name = "cblagden", email = "chase.blagden@gmail.com" }]
