[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tee_selector"
version = "0.4.0"
description = "A basic tee selector"
authors = [{ name = "Brock Weaver", email = "brockweaver@gmail.com" }]
license = { text = "MIT" }
dependencies = []  # Add dependencies if needed

[project.urls]
"Homepage" = "https://github.com/brockweaver/tee-selector"
"Bug Tracker" = "https://github.com/brockweaver/tee-selector/issues"

[tool.setuptools.packages.find]
where = ["."]