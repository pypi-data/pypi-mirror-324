[project]
name = "mcp-x-post"
version = "0.1.0"
description = "A MCP server project for posting tweet on X"
readme = "README.md"
requires-python = ">=3.12.5"
dependencies = [
 "httpx>=0.28.1",
 "mcp>=1.2.1",
 "python-dotenv>=1.0.1",
]
[[project.authors]]
name = "ushishu"
email = "shuzo.1128@gmail.com"

[build-system]
requires = [ "hatchling",]
build-backend = "hatchling.build"

[project.scripts]
mcp-x-post = "mcp_x_post:main"
