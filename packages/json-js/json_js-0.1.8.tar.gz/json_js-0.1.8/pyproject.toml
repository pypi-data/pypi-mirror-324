[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "json_js"
version = "0.1.8"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
exclude = ["src/__pycache__", "src/json_js/__pycache__" ,"src/json_js.egg-info"]
