from setuptools import setup

setup(
    name="Cayley_perms",
    version="1.0",
    description="A module for using Cayley permutations.",
    #    author='',
    #    author_email='foomail@foo.example',
    packages=["cperms"],  # same as name
    # install_requires=["typing"],  # external packages as dependencies
)
