[project]
name = "cli-options"
version = "0.2.0"
description = ""
authors = [
    {name = "Ed Scrimaglia",email = "edgardo.scrimaglia@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "typer (>=0.15.1,<0.16.0)"
]

[project.scripts]
cli-options = "cli_options.main:app"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
