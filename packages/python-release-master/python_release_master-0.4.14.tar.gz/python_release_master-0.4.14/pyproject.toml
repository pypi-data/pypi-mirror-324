[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "python-release-master"
version = "0.4.14"
description = "An automated Python package release management tool with AI-powered changelog generation"
readme = "README.md"
requires-python = ">=3.8"
license = "MIT"
keywords = ["release", "automation", "changelog", "pypi", "ci", "cd"]
authors = [
    { name = "Kareem Elbahrawy", email = "kareem.elbahrawy@gmail.com" }
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "click>=8.0.0",
    "openai==1.12.0",
    "httpx>=0.24.1",
    "pyyaml>=6.0.0",
    "requests>=2.25.0",
    "rich>=10.0.0",
    "typer>=0.9.0",
    "toml>=0.10.0",
    "uv>=0.1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "black>=23.0.0",
    "isort>=5.0.0",
    "flake8>=6.0.0",
    "mypy>=1.0.0",
    "sphinx>=6.0.0",
    "sphinx-rtd-theme>=1.0.0",
]

[project.scripts]
python-release-master = "python_release_master.cli:main"

[tool.black]
line-length = 100
target-version = ["py38"]

[tool.isort]
profile = "black"
line_length = 100
multi_line_output = 3

[tool.mypy]
python_version = "3.8"
strict = true
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q --cov=python_release_master"
testpaths = ["tests"]

[tool.hatch.build]
include = [
    "src/python_release_master/**/*.py",
    "src/python_release_master/**/*.pyi",
    "tests/**/*.py",
]
packages = ["src"]

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.hatch.build.targets.wheel.sources]
"src" = "python_release_master"
