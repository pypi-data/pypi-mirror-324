[project]
name = "gstin-validator"
version = "0.1.0"
description = "validate india GSTIN numbers. return True if valid, false otherwise."
authors = [
    {name = "mohammed.saleel0099@gmail.com"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.4"

