[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "welcome_derto"
version = "0.0.2"
authors = [
  { name="Cosimo Di Martino", email="dertranslations@gmail.com" },
]
description = "Test package, used to print a welcome message depending on user in my organization."
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = "MIT"
license-files = ["LICEN[CS]E*"]

[project.urls]
