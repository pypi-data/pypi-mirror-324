[build-system]
name = "solver-multiRPC"
requires = [
    "setuptools>=42",
    "wheel"
]
version = "3.1.2"
dependencies = [
    "web3>7.0.0",
    "multicallable>=6.0.0",
    "eth-account>=0.13.0",
]
build-backend = "setuptools.build_meta"