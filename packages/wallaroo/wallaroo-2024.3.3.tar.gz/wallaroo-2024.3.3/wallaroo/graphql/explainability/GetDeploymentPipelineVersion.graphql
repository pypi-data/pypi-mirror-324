query DeploymentPipelineVersion($id: bigint!) {
    pipeline(where: {id: {_eq: $id}}) {
        id
        deployment {
        deployment_pipeline_versions {
            pipeline_version {
            version
            }
        }
        }
    }
}
