[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rlaif"
version = "0.0.1"
description = "RLAIF tooling"
authors = [{ name = "cblagden", email = "chase.blagden@gmail.com" }]
