[project]
name = "quantmax"
version = "0.0.1"
description = "Quantization made with Max efficiency."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true


[project.license]
file = "LICENSE"  # If you have a LICENSE file