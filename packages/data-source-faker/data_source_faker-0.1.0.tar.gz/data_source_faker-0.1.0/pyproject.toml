[tool.poetry]
name = "data-source-faker"
version = "0.1.0"
description = ""
authors = ["dfunto <dadutra2@gmail.com>"]
readme = "README.md"
packages = [{include = "data_source_faker", from = "src"}]

[tool.poetry.dependencies]
python = "^3.10"
faker = "^33.1.0"
boto3 = "^1.35.81"
pandas = "^2.2.3"
schedule = "^1.2.2"
pyarrow = "^18.1.0"
fastparquet = "^2024.11.0"
pyyaml = "^6.0.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
