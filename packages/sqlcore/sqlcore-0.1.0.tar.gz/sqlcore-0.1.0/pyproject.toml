[tool.poetry]
name = "sqlcore"
version = "0.1.0"
description = "A simple, lightweight DB connector that allows queries and interactions through raw SQL"
authors = ["Alastair Grant <85125580+aldo-g@users.noreply.github.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
pyodbc = "^5.2.0"
pytest-asyncio = "^0.25.3"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.4"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"