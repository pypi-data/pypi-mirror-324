[tool.poetry]
name = "ipodnano-scrobbler"
version = "0.1.0"
description = "iPod Nano 6th Gen last.fm scrobbler"
authors = ["Your Name <you@example.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.7"
pandas = "*"
pyarrow = "*"
polars = "*"
tqdm = "*"
trainerlog = "*"
requests = "*"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
my-script = "ipodnano_scrobbler.read_dbs:main"