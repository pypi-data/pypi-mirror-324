[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "verifiable-envs"
version = "0.0.1"
description = "Verifiable envs."
authors = [{ name = "cblagden", email = "chase.blagden@gmail.com" }]
